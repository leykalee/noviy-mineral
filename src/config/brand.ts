/**
 * Источник логотипа — одно место на весь проект.
 *
 * Файлы получены с аватарки сообщества VK (vk.ru/noviy_mineral, id 215855991).
 * Логотип НЕ перерисовывался: знак вырезан из оригинала, фон переведён в прозрачность.
 * Чтобы заменить логотип, достаточно положить новые файлы по этим путям.
 */
export const brandAssets = {
  /** Знак (кристалл + шеврон) на фирменной плашке — хедер, футер */
  markBadge: '/brand/logo-mark-badge.png',
  /** Знак с прозрачным фоном — для тёмных подложек */
  mark: '/brand/logo-mark.png',
  /** Полная аватарка без изменений — OG, соцсети, favicon-источник */
  avatar: '/brand/logo-avatar.png',
  avatarLarge: '/brand/logo-avatar.jpg',
  /** Обложка сообщества VK */
  cover: '/brand/vk-cover.jpg',
  /** Логотип получен автоматически. false → на сайте текстовый fallback */
  isRealLogo: true,
} as const;

/**
 * Фирменные цвета, снятые пиксельным анализом оригинальной аватарки,
 * а не подобранные на глаз (п.50 ТЗ).
 */
export const brandColors = {
  /** Фон-поле логотипа, 82.4 % площади аватарки */
  deep: '#0C676A',
  /** Заливка кристалла и шеврона */
  turquoise: '#2AB8C6',
  white: '#FFFFFF',
} as const;
